# ルミナ 歩行グラフィック試作 06

正面（マップ下方向）と右向き。部位素材を組み合わせた8コマの歩行ループです。

## 今回の修正

- 右向きの首パーツ上端に残っていた角を落としました。上端を細くし、胸元へ向かって連続して広がる輪郭にしています。顎の下の小さな張り出しを除く修正です。
- 右向きの両手を、指の関節が軽く曲がった形に描き直しました。伸びていた指先を短く丸め、手首にも12〜16度の曲げを加えています。
- 正面の全8コマ、右向きの頭・翼・脚の動き、肩と肘の動きは試作05と同じです。服の胴部分も保持しています。
- 1歩4コマ、1周期8コマ、1コマ120 ms（約8.33 fps）。補間はしません。

## 再生

`lumina-walk-player.html` をブラウザーで開いてください。画像は埋め込み済みです。再生・停止、1コマ送り、フレーム選択、速度変更、部位一覧を操作できます。OSの動きを抑える設定が有効なら停止状態で開きます。

`lumina-walk-preview.gif` は正面と右向きの背景付きGIF、`lumina-right-preview.gif` は右向きのみの確認用GIFです。影とマス目は確認用で、素材PNGには含みません。

## 素材

| ファイル | 内容 |
| --- | --- |
| lumina-walk-spritesheet.png | 8列×2行、1536×512 px。上段が正面、下段が右向き。各行を左から右へ再生 |
| lumina-front-8frames.png / lumina-side-8frames.png | 方向別の8コマ横並び |
| lumina-front-walk.png / lumina-side-walk.png | 透過APNG。非対応ビューアーでは先頭コマのみ表示 |
| frames/front/、frames/side/ | 各方向の独立した透過PNG、00〜07 |
| parts/front/、parts/side/ | 組み立て用PNG。横向きの腕・脚は元画像と各8種類の変形済み画像を含む |
| rig.json | 部位画像、支点、座標、角度、スケール、描画順、腕の奥行き、足首角度、手首の曲げ角度 |
| source-atlases/ | 元の部位素材、腕・後ろ髪の修正版、今回の首・手の修正版 |
| generation-prompts*.txt | 素材生成のプロンプト |

各コマ192×256 px、透過RGBAです。足元の地面は y=242。マップ上でのキャラクター移動は含まない、その場歩き素材です。頭と輪は一体、前髪は頭に含みます。

far / near は、正面では画面左 / 右、横向きでは奥側 / 手前側を表します。右向きの両脚の足先はいずれも画面右へ向いています。

## 調整・再出力

Python 3、Pillow、NumPy が必要です。

```bash
python -m pip install -r requirements.txt
python rebuild.py
```

同じフォルダーのPNG、GIF、リグ、プレイヤーを上書きします。`pose_front` の腕の奥行き、`pose_side` の胴体姿勢と足首角度、`whole_arm` / `whole_leg` の連続変形、`DURATION` のコマ時間を調整できます。

リグは各フレームの配列順に背面から描画します。ローカル支点を引き、スケール、回転、座標移動の順で変換します。角度は度単位です。`hierarchy` は連動関係の説明で、描画用の変換値は計算済みです。

## 確認した内容

全8コマを拡大し、首元の輪郭と手首・指先の形を確認しました。首の前側の輪郭は、上から下へ途切れず広がっています。正面の8枚のPNGと、右向きの脚の画像・移動値は試作05と同一です。頭・翼・肩・肘の位置も同じです。右向きの腕16枚には、穴や分離した画素がありません。GIF・APNGはいずれも8コマ、120 msです。

Canvasによる再生、停止、コマ送り、速度変更、フレーム選択、部位表示を確認しました。確認にはNodeのCanvas実装を使用しており、ブラウザー上のページレイアウトは未検証です。同梱の再生成スクリプトでも、同一のコマ画像・プレビュー・リグ・プレイヤーを再出力できることを確認しています。

素材の描画には組み込み画像生成ツールを使用し、切り出し・部位の変形・アニメーションの組み立てにはコードを使用しています。
